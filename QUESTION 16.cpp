// QUESTION 16

// The logical NOT operator (!) reverses a boolean value(true becomes false, false becomes true).
// Example: bool a = true;
// cout << !a; outputs 0(false)

// The bitwise NOT operator(~) flips every bit of a number(0s become 1s and 1s become 0s).
// Example: int b = 5;
// cout << ~b; outputs -6
