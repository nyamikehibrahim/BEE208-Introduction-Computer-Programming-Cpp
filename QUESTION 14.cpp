// QUESTION 14

// The left shift operator(<<) shifts the bits of a number to the left, which multiplies the number by 2 for each shift.
// Example: int x = 5;
// cout << (x << 1); outputs 10(5 multiplied by 2)

// The right shift operator(>>) shifts the bits of a number to the right, which divides the number by 2 for each shift.
// Example: int y = 10;
// cout << (y >> 1); outputs 5(10 divided by 2)
